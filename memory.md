# Memory — Gryphon Group (лендінг лізингу)

Стабільні бізнес-факти про проєкт. Тільки сталі речі.

## Бізнес

- **Компанія:** Gryphon Group Sp. z o.o.
- **Юр. дані:** NIP 8133823354 · REGON 384467200 · KRS 0000805925 · реєстр кредитних посередників RPK047401.
- **Siedziba (юр. адреса):** ul. Aleja Armii Krajowej 80 lok 18, 35-307 Rzeszów, Polska.
- **Адреса для кореспонденції:** ul. Św. Rocha 45A/1, 35-330 Rzeszów.
- **Контакти:** тел. +48 516 929 619 · e-mail admin@gryphongroup.pl.
- **Продукт:** лізинг автомобілів у Польщі для іноземців та бізнесу.
- **Ключові переваги (USP):**
  - без карти побиту (для іноземців);
  - без перевірки доходів;
  - рішення за 15 хвилин;
  - авто на руках за 7 днів;
  - нові та вживані авто, знижки до 30% завдяки партнерствам з дилерами;
  - співпраця з 15+ лізинговими фірмами та банками.
- **Цільова аудиторія:** підприємці й малий бізнес; водії-фрілансери (кур'єри, таксі, логісти); IT та фахівці з доходом; українці та білоруси в Польщі.

## Бренд

- **Логотип:** золотий квадрат (#B0823C) з трьома білими «перами» крила + вордмарк «GRYPHON GROUP». Файл: `assets/logo.png` (пера прозорі — на сторінці підкладено біле тло).
- **Колір назви фірми у шапці/футері:** темно-коричневий `#4A3418` (один колір на обидва рядки).
- **Палітра:** кремова основа `#F7F2E9`, золото-бронза `#B0843B`, темні акцентні секції `#16110C`.
- **Шрифти:** заголовки — Fraunces (серіф), текст — Manrope.
- **Мови:** українська (за замовчуванням) + російська, перемикач UA/RU у шапці.

## Калькулятор лізингу

- **Ефективна річна ставка:** 8,2694% (місячна ≈ 0,6643%; номінальна ~7,97% = WIBOR 3,81% + маржа ~4,16%). ✅ Відкалібровано 13.06.2026 під реальну рату партнера: 180 000 брутто / внесок 10% / викуп 25% / 60 міс → 2 702,42 PLN брутто (наш калькулятор дає 2 702 ✓).
- **VAT 23%** (нетто → брутто): ціна вводиться брутто, нетто = брутто/1,23. Рата показується у двох значеннях: брутто і нетто.
- **Структура (звірено зі скрином партнера 13.06.2026):** opłata wstępna = перший платіж при підписанні, тому **кількість рат = термін − 1** (60 міс → 59 рат). Дефолт викупу — 20% (діапазон 1–45%).
- **Формула:** ануїтет із залишковою вартістю (викуп як balloon наприкінці). Розрахунок орієнтовний, не є офертою.
- **Звірка на параметрах партнера** (брутто 147 600 / нетто 120 000, внесок 10%, викуп 25%, 60 міс): наш калькулятор дає opłata wstępna 14 760, wykup 36 900, 59 рат — **точний збіг зі скрином**; місячна рата виходить ~1 931 нетто / 2 375 брутто (це і є число для звірки з реальною ратою партнера).

## Каталог авто (4 категорії)

1. **Легкові преміум** — виділена («featured»: золота рамка + бейдж-зірка).
2. **Легкові авто** — стандарт/комфорт-клас.
3. **Бусі для бізнесу.**
4. **Вантажівки і зестави.**

## Технічне

- Лендінг: один самодостатній файл `gryphon-website/index.html` (CSS у `<style>`, JS у `<script>`); залежності — Google Fonts + локальні зображення в `assets/` (усі WebP: `logo.webp`, hero `car-hero-bmw.webp`, бус `van-mercedes-bg.webp`, каталог `car-premium/standard/van.webp` + вантажівка `car-truck-volvo.webp`).
- Знімок робочого варіанта станом на 2026-05-23: `gryphon-website/index.backup-2026-05-23.html`.
- **Форма заявки:** приймач — Google Apps Script (пише в Google Sheet + шле в Telegram-групу одним запитом; токен бота на сервері Google, не в коді сайту). Код скрипта + інструкція: `gryphon-website/integrations/google-apps-script.gs` і `README-setup.md`. У `index.html` змінна `LEAD_ENDPOINT` заповнена робочим Web App URL. Обробник: реальна обробка помилок (екран «Не вдалося відправити» із запасними контактами замість фейкового «дякуємо»), honeypot-поле `company_extra`, відправка form-encoded (без preflight).
  - **Налаштовано у скрипті (вписано користувачем):** `SHEET_ID = 1uLfIVEBWJ1balWa6Z7bY8nKa-Le8lGxUITw3Kq5rDVI`; `SHEET_NAME = 'Заявки лізинг'`; `TELEGRAM_CHAT_ID = -1003988229437` (Telegram-група, працівники додані, бот — адмін групи); `TELEGRAM_BOT_TOKEN` — вписаний у рядок 8 скрипта, але **засвічений у скриншоті → ПЕРЕВИПУСТИТИ через @BotFather `/revoke` і замінити**.
  - **Акаунт:** скрипт і таблиця під особистим Gmail `konoplyovaliliia@gmail.com`. Apps Script прив'язано до стандартного Google Cloud project `My First Project` (`project number 968177333470`) після OAuth consent screen + test user.
  - **Стан на 13.06.2026:** приймач заявок ЗРОБЛЕНО. Блок Google Apps Script знято через OAuth consent screen у стандартному GCP-проєкті `My First Project` (`project number 968177333470`) + test user `konoplyovaliliia@gmail.com`; Apps Script прив'язано до цього GCP-проєкту. Web App URL вставлено в `LEAD_ENDPOINT` у `index.html`: `https://script.google.com/macros/s/AKfycbzmLWw0U4tobb1F6c9rRwpP_7krOtHleNyJMS-nJ7NcY0WgtqYyUZGe3NMjlDOliU-cZg/exec`. GET перевірка повертає `{"ok":true,"status":"alive"}`, тестові POST повертають `{"ok":true}`. Telegram-група підтверджена користувачем: тестова заявка прийшла. Google Sheet підтверджено: тестові заявки записались у лист `Заявки лізинг`, телефон `+48 516 929 619` записався без `#ERROR!`. Реальний тест із лендингу `index.html#final` спрацював. Запасне TG-посилання замінено на `https://t.me/Gryphon_Group`. Опційно після запуску: перевипустити засвічений токен бота.
- **Політика приватності (RODO):** вшита як модал у `index.html` (текст UA, секція `#privacyModal`); посилання у футері та біля чекбокса згоди у формі. Форма має обов'язковий чекбокс згоди (`#consentMain`, required) + опційний маркетинговий (`#consentMkt`). Дата редакції в тексті — 13.06.2026.
- **Деплой:** GitHub Pages увімкнений для репозиторію `Liliia2/gryphon-website`, гілка `main`, root `/`. Публічна адреса: `https://liliia2.github.io/gryphon-website/`. Останній push після інтеграції форми: `e025db2 Wire lead form receiver`.
- **Meta Pixel (ЗРОБЛЕНО):** Pixel ID `713530188067823` (набір даних «AutoLeasing», портфоліо AutoBiznes_Leasing). Вшито в `index.html`, опубліковано (коміт `a8eee7b`). Вантажиться **лише після згоди** через cookie-банер. Події: `PageView` (після згоди) і `Lead` (на успішну відправку форми — функція `ggTrackLead()` у success-гілці). Перевірено реальними запитами до Meta: PageView і Lead → 200. До згоди / після «Відхилити» Pixel мовчить. Лишилось після підняття домену: перевірити в Meta Pixel Helper на живій сторінці + зробити **підтвердження домену в Meta** (потрібно для реклами; github.io підтвердити не можна, треба свій домен).
- **Cookie-банер:** `#cookieBanner` (UA/RU), кнопки `#cookieAccept`/`#cookieDecline`; згода в `localStorage` ключ `gg_consent` = `granted`/`denied`. Потрібен бо політика RODO каже: маркетингові/аналітичні cookie — лише після згоди.
- **Власний домен (В ПРОЦЕСІ — застрягли на DNS TheHost):** цільова адреса для реклами `https://leasing.gryphongroup.pl/` (під-домен; головний `gryphongroup.pl` зайнятий сайтом компанії — НЕ чіпати). Спосіб: файли лишаються на GitHub Pages, під-домен націлено через DNS. Файл `CNAME` у репо = `leasing.gryphongroup.pl` (коміт `be66c53`). Домен+хостинг у TheHost (панелі: my.thehost.com.ua білінг + s22.thehost.com.ua ISPmanager; NS ns1-4.thehost.com.ua). Користувач додала запис у ISPmanager → «Доменные имена» → gryphongroup.pl → Записи: `leasing CNAME liliia2.github.io.` (видно в панелі). **ПРОБЛЕМА:** авторитетний `ns1.thehost.com.ua` віддає зону (A/MX/www ок), але запис `leasing` ще не повертає → `leasing.gryphongroup.pl` = NXDOMAIN. Ймовірно затримка синхронізації на боці TheHost або не та панель. Користувачу дано текст звернення в підтримку TheHost. **CNAME-файл ТИМЧАСОВО прибрано з репо (коміт `7181782`)**, бо поки він був, github.io робив 301 на неробочий домен і сайт лежав. Зараз сайт живий на `https://liliia2.github.io/gryphon-website/` без власного домену. **Причепити домен назад** (коли запрацює): спершу перевірити `nslookup leasing.gryphongroup.pl ns1.thehost.com.ua` → має вернути `liliia2.github.io`; тоді знову створити файл `CNAME` (один рядок `leasing.gryphongroup.pl`) + push. НЕ додавати CNAME до того, як DNS запрацює (інакше сайт знову ляже). Примітка: «зникле лого» 13.06 — не баг (глюк прев'ю-панелі + кеш 301-редіректу); лого і всі картинки цілі.

## Генерація візуалу (інструменти)

- **Higgsfield** підключений як custom MCP (план Plus). Використовуємо для фото/відео авто.
  - Модель для фото каталогу: `marketing_studio_image` (формат 3:2, 2K, ~2 кредити/кадр).
  - Застереження: AI тяжіє до реальних моделей і логотипів навіть при «no logos» — для конкретних брендів каталогу надійніше реальні фото.
- **Gemini API** підключений через ключ у `gryphon-website/.env` (`GEMINI_API_KEY`, файл у `.gitignore`).
  - Текст працює на безкоштовному тарифі.
  - **Генерація зображень (Nano Banana) потребує увімкненого billing** у проєкті Google Cloud — підписка «Pro» на застосунок Gemini цього НЕ відкриває (це різні речі). Поки не ввімкнено → лишаємось на Higgsfield.
