# Plan — Gryphon Group (лендінг лізингу)

## Поточний стан

Лендінг зібрано і працює: `gryphon-website/index.html`.
Структура секцій: шапка (sticky, UA/RU) → hero (фон — фото BMW 7) → смуга довіри → секція «Калькулятор» (окремий блок, фон — бус Mercedes) → каталог авто (4 картки) → «Для кого» (темна) → «Як це працює» (5 кроків) → «Що входить» → FAQ → фінальний CTA з формою (темна) → футер.
Динаміка: поява секцій при скролі, hover-ефекти, sticky-nav, живий калькулятор, перемикач мов із запам'ятовуванням.

## Зроблено

- Узгоджено напрямок: світла преміальна основа + темні акцентні секції.
- Інтегровано офіційний логотип (`assets/logo.png`, вирізано квадрат-крило для шапки).
- Назва фірми — один темно-коричневий колір `#4A3418`.
- Калькулятор: ефективна ставка 10,5% річних, переплата за 5 років ~20-25%; ціна та ставка — брутто.
- Каталог: 4 картки, преміум виділена; адаптив 4→2→1 без «осиротілих» карток.
- Узгоджено копірайт: скрізь «нові та вживані», знижки «до 30%».

## Наступні дії

1. **КРИТИЧНО — приймач заявок ЗРОБЛЕНО:** приймач заявок = Telegram-група + Google Sheet через Google Apps Script. Деплой Web App пройшов після налаштування OAuth consent screen + test user у стандартному GCP-проєкті. Новий URL після правки телефону вставлено в `LEAD_ENDPOINT`, технічні GET/POST перевірки успішні. ✅ Telegram-група підтверджена користувачем: тестова заявка прийшла. ✅ Google Sheet підтверджено: тестові заявки записались у лист `Заявки лізинг`. ✅ Телефон `+48 516 929 619` у рядку `Тест телефон Codex` записався без `#ERROR!`. ✅ Запасне Telegram-посилання замінено на `https://t.me/Gryphon_Group`. ✅ Реальний тест із лендингу `index.html#final` спрацював: форма відправляє заявку. Залишилось опційно: перевипустити засвічений токен бота після запуску.
   - ✅ ЗРОБЛЕНО: код приймача `integrations/google-apps-script.gs` (Sheet + Telegram одним запитом) + інструкція налаштування.
   - ✅ ЗРОБЛЕНО: переписано обробник форми — реальна обробка помилок (екран «Не вдалося відправити» + запасні контакти), honeypot проти спаму, відправка form-encoded.
   - ✅ ЗРОБЛЕНО: калькулятор приведено до логіки партнера + ставку відкалібровано (8,2694%).
   - ✅ ЗРОБЛЕНО: політика приватності RODO (модал UA) + чекбокси згоди у формі.
   - ✅ ЗРОБЛЕНО: контакти у футері (тел. +48 516 929 619, admin@gryphongroup.pl), юр. дані (NIP/KRS), індекс виправлено на 35-307.
   - ✅ ЗРОБЛЕНО 13.06.2026: блок Google Apps Script знято. Створено OAuth consent screen у стандартному GCP-проєкті `My First Project`, додано test user `konoplyovaliliia@gmail.com`, Apps Script прив'язано до GCP project number `968177333470`, деплой Web App успішний. URL вставлено в `LEAD_ENDPOINT` у `index.html`. GET перевірка повертає `{"ok":true,"status":"alive"}`, тестовий POST повертає `{"ok":true}`. ЗАЛИШИЛОСЬ: користувачу підтвердити, що тестова заявка "Тест Codex" з'явилась у листі `Заявки лізинг` і прийшла в Telegram-групу; замінити запасне TG-посилання `https://t.me/CHANGE_ME`; бажано перевипустити засвічений токен бота після фінального тесту.
2. Вирішити: ставка нетто чи брутто (зараз обидві брутто; для B2B можлива схема «ціна брутто, рата нетто»).
3. Продовжити правки дизайну за чергою (керує користувач).
4. Опційно: точніший серіф під макет, реальні фото авто замість силуетів, деплой/хостинг.

## Як перевірити результат

- Відкрити `gryphon-website/index.html` подвійним кліком (або прев'ю-сервер `gryphon` на :8765).
- Калькулятор миттєво рахує ставку; зміна ціни/внеску/терміну/викупу оновлює результат.
- Перемикач UA/RU міняє весь текст і запам'ятовує вибір.
- Каталог: 4 картки в ряд на широкому екрані, 2×2 на середньому, 1 на мобільному.
- Форма: після відправки показує екран подяки; заявка доходить у реальний приймач (після п.1).

## Візуал та оптимізація (зроблено, версія 2026-05-23)

Знімок цього варіанта: `index.backup-2026-05-23.html`.

- **Hero:** фон — фото BMW 7 (`assets/car-hero-bmw.webp`) з градієнтним затемненням зліва; калькулятор винесено з hero.
- **Калькулятор:** окрема секція `#calculator` після смуги довіри; фон секції — бус Mercedes Sprinter (`assets/van-mercedes-bg.webp`, вуаль 88%); картка напівпрозора (білий 85%).
- **Каталог:** 4 реальні фото замість силуетів (`car-premium/standard/van.webp`); вантажівка — Volvo FH сірий з графікою (`car-truck-volvo.webp`).
- **Шрифти:** збільшені по всій сторінці; вторинний колір `--muted` потемнішав для контрасту.
- **Заголовки секцій:** усі центровані.
- **Зображення:** усі стиснуті в WebP (~37 МБ → ~0,5 МБ); логотип — `logo.webp`.
- **Показ контенту:** надійний reveal (показ на завантаженні + при скролі) — виправлено баг «порожній екран».
- **Адаптив:** перевірено мобільний; кнопка форми на телефоні показує лише «Залишити заявку».
- Gemini-фото відкладено: потрібен billing у Google Cloud (див. `memory.md`).
